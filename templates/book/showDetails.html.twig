
<div align="center">
<h1>Book</h1>

    <table align="center" border="1">
        <tbody>
            <tr>
                <th>Title</th>
                <td>{{ book.title }}</td>
            </tr>
            <tr>
                <th>Publication date</th>
                <td>{{ book.publicationDate|date }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td> {{book.author.username}} </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('list_book') }}">back to list</a>

    <a href="{{ path('book_edit', {'id': book.ref}) }}">edit</a>
</div>